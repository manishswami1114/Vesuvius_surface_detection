# Vesuvius - Segmentation MLOps Project

Repository that contains training and inference pipelines for the Vesuvius Surface Detection challenge.
Structure provides modular training, inference, robust post-processing, and CLI.

## Quickstart

1. Edit `configs/default.yaml` to point to your dataset and model paths.
2. Install deps:
   ```bash
   pip install -r requirements.txt
